AppArmor only restricts tasks permission sets it does not provide
a way to raise a tasks capability set, however it can be used in
conjunction with other methods to raise a tasks capability set.

setuid/setguid

file capabilities

pam\_cap module
